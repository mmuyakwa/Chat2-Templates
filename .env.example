#
# This is an example environment file.
# Copy this file to .env and fill in your actual values.
# The .env file is ignored by git and should NOT be committed.
#

# --- Flask Settings ---
# Tells Flask how to load the application. Do not change this.
FLASK_APP=app.app:create_app

# Sets the port for the local development server.
FLASK_RUN_PORT=5004

# Set to 'true' for development to enable auto-reloading and the debugger.
FLASK_DEBUG=true

# A strong, random secret key is required for session security.
# Generate one with: python -c 'import secrets; print(secrets.token_hex(24))'
SECRET_KEY=your_strong_secret_key_here

# --- API Keys ---
# Get your free API key from https://openrouter.ai/keys
OPENROUTER_API_KEY=your_openrouter_api_key_here

# --- Application-Specific Settings ---
# Sets the default model selected on page load. Must be a valid model ID from OpenRouter.
DEFAULT_MODEL=openai/gpt-4o-mini

# For LOCAL DEVELOPMENT (when not using Docker), uncomment the line below.
# This tells the app where to find the prompts folder relative to the project root.
# For Docker, leave this commented out, as the docker-compose.yaml volume handles the path.
# PROMPTS_PATH=prompts
